# Expense-tracker
FINTRACK PRO is a Python CLI expense tracker using SQLAlchemy and SQLite. It lets users add, update, delete, and search expenses, manage categories, set monthly budgets, and view reports. Data is stored locally with budget warnings and simple command-line controls for efficient personal finance tracking.
